# Landing page components

Keep the public Notifyr marketing experience in this feature folder.

- `layout/` contains the marketing header, footer, and navigation.
- `sections/` contains page-level narrative sections such as hero, workflow, proof, and calls to action.
- `visuals/` contains the animated product demonstration and other landing-specific visual compositions.

Components shared with the authenticated application belong in `components/ui/` instead.
